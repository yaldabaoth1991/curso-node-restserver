#notas Webserver +rest

recuerden que hay que ejecutar `npm install` para  reconstruir los modulos de node
git init                      
git add . 
git commit -m "Webserver listo" 

git remote

 git remote add origin https://github.com/yaldabaoth1991/curso-node-restserver.git
 git branch -M main
 git push -u origin main